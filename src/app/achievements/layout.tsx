export const metadata = {
  title: "Om Londhe | Achievements",
  description: "Achievements showcase for Om Londhe's portfolio.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <section>{children}</section>;
}
